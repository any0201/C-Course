#include<stdio.h>
//CALL BY VALUE - TUTORIAL 19
//CALL BY REFERENCE

/*void changeValue(int* address)
{
    *address = 345;
}
int main()
{
    int a=34, b=56;;
    printf("the value of a now is %d\n",a);
    changeValue(&a);
    printf("the value of a now is %d\n",a);
    return 0;
}
*/
//QUIZ:
// GIVEN TWO NUMBERS A AND B, ADD THEM THEN SUBTACT THEM AND ASSIGN TO A AND B USING CALL BY REFERENCE
/*a=4
b=3
after
a=7
b=1*/